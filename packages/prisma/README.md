# Shared database package

Prisma 7 with the PostgreSQL driver adapter. Owns the database schema, initial SQL migration, seed and generated client exports. Money uses Decimal(10,2). Order items snapshot names and prices. See the root README for migration and seed commands. Never use `db push` as a substitute for reviewed production migrations.
