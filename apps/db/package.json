{
  "name": "db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up -d postgres || true",
    "start": "docker-compose up -d postgres || true",
    "stop": "docker-compose down",
    "down": "docker-compose down",
    "logs": "docker-compose logs -f postgres",
    "ps": "docker-compose ps"
  }
}
